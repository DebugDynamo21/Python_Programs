# TUPLES - A built in datatype that allows us to create 
# immutable(that cannot be changed) sequences of values.

tup = (1, 2, 3, 4)
print(tup)
print(type(tup))
print(tup[2])

# any particular index value in tuple can be accessed 
# but it cannot be modified just like in string.